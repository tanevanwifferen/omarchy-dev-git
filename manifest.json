{
  "schemaVersion": 1,
  "id": "dev.git",
  "name": "Git",
  "version": "2.2.0",
  "author": "ariadev",
  "license": "MIT",
  "description": "GitHub, GitLab and Gitea open work: a full-year contribution graph, review queue, failing checks and pipelines, and your open pull/merge requests in a native Omarchy bar panel.",
  "kinds": [
    "bar-widget"
  ],
  "activation": "on-demand",
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Git",
    "description": "One bar icon and one panel: per-provider full-year activity graph, a grid of review/assignment counts, build state on every request, and click-to-open queues of merge requests and issues. Multiple hosts per provider (GitHub Enterprise, self-managed GitLab and Gitea) switch inside the tab.",
    "category": "Dev",
    "aliases": [
      "git",
      "github",
      "gitlab",
      "gitea",
      "merge-requests",
      "pull-requests",
      "pipelines",
      "actions",
      "ci"
    ],
    "allowMultiple": false,
    "defaults": {
      "providers": {
        "github": {
          "enabled": true
        },
        "gitlab": {
          "enabled": true
        },
        "gitea": {
          "enabled": true
        }
      },
      "refreshIntervalSec": 300
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh interval (seconds)",
        "min": 30,
        "max": 3600,
        "step": 30,
        "defaultValue": 300
      }
    ]
  }
}
